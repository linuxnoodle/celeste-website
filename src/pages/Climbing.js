import React from 'react';
import styled from 'styled-components'

const textContent = styled.p`
    font-size: 20px
`;

const Home = () => {
  return (
    <textContent>
    </textContent>
  );
};
  
export default Home;
